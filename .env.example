# ===========================
# Qwip – Variáveis de Ambiente
# Copie para .env.local em dev e configure no Vercel (Production/Preview)
# ===========================

# ----- Público (pode ir para o client) -----
# Telefone do WhatsApp em E.164 sem "+" (só dígitos, ex.: 55DDDNUMERO)
NEXT_PUBLIC_WHATSAPP_PHONE=5599999999999
# Turnstile (Cloudflare) – site key pública
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# ----- Segredos do servidor -----
# Segredo único do projeto (>=32 chars). Usado por sessão e tokens.
SIGNING_SECRET=change-me-please-32chars-or-more
# Segredo para assinar nonces temporários
QWIP_NONCE_SECRET=change-me-too-32chars-or-more

# ----- Twilio Verify (OTP via SMS) -----
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# ----- Google Cloud Vision (moderação de imagem) -----
# Caminho 1 – mais simples na Vercel: API KEY (REST)
GOOGLE_VISION_API_KEY=

# Caminho 2 – opcional: Service Account (SDK) se preferir
# (Cole a private key com \n literais; o código converte)
GCP_PROJECT_ID=
GCP_CLIENT_EMAIL=
GCP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nSEU-PRIVATE-KEY-AQUI\n-----END PRIVATE KEY-----\n"

# Tuning do Vision (opcional). Se STRICT=true, falha da API bloqueia publicação.
SAFE_VISION_STRICT=false
SAFE_VISION_DEBUG=false
SAFE_VISION_ADULT_MIN=LIKELY
SAFE_VISION_VIOLENCE_MIN=LIKELY
SAFE_VISION_RACY_MIN=VERY_LIKELY

# ----- Banco de Dados (Prisma) -----
# Ex.: postgresql://user:pass@host:5432/db?schema=public
DATABASE_URL=

# ----- URL Base do app (opcional; usada em links/CORS) -----
NEXT_PUBLIC_BASE_URL=https://qwip.pro

# ----- Vercel Blob (storage de imagens) -----
BLOB_READ_WRITE_TOKEN=
