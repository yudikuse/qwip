// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // já está setado na Vercel
}

// Nenhum model ainda – só para habilitar prisma generate sem erro.
